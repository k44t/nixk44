# IMPORTS
import argparse
# IMPORTS_END

# MAIN
parser = argparse.ArgumentParser(
        prog='#PROGRAM_NAME',
        description='#PROGRAM_DESCRIPTION',
        epilog='In case of Problems, Bugs or for Feature Requests, feel free to get in touch with me <apoeschlfreelancing@kwanta.net>.'
    )
    parser.add_argument('--verbose',
                        dest='verbose',
                        action='store_const',
                        const="verbose",
                        default=False,
                        help='output verbose'
                        )
    parser.add_argument('--silent',
                        dest='silent',
                        action='store_const',
                        const="silent",
                        default=False,
                        help='silence output'
                        )
    parser.add_argument('--#STRING_ARGUMENT',
                        dest='#STRING_ARGUMENT',
                        default="#DEFAULT_STRING_ARGUMENT",
                        type=str,
                        help='#HELP_STRING_ARGUMENT'
                        )
    args, unknown = parser.parse_known_args()
    if None != unknown:
        log_error(f"unknown arguments: {unknown}")
    verbose = args.verbose
    silent = args.silent
    #STRING_ARGUMENT = args.#STRING_ARGUMENT
# MAIN_END